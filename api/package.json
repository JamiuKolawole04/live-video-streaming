{
  "name": "api",
  "version": "1.0.0",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "concurrently --kill-others \"yarn watch\"  \"nodemon .\" "
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.21",
    "tslint": "^6.1.3",
    "typescript": "^4.9.5"
  }
}
